#
# <%= node[:chef_splunk][:home] %>/<%= @splunk_conf_base %>/<%= @splunk_app %>/<%= @dir %>/<%= @conf %>
#
# Generated by Chef for <%= node[:fqdn] %>
#

<% @stanzas.each do |stanza, kvpairs| %>
<% if stanza == '[]' %>
[]
<% else %>
[<%= "#{stanza}" %>]
<% end %>
	<% kvpairs.each do |key, value| %>
<%= "#{key} = #{value}" %>
	<% end %>
	
<% end %>
